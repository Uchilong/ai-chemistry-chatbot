# Minimal requirements for faster startup
streamlit>=1.28.0
Pillow>=9.0.0
python-dotenv>=1.0.0
requests>=2.28.0
pydantic>=2.0.0

# AI SDKs (only what you need)
# google-generativeai>=0.3.0  # Uncomment if using Gemini
# mistralai>=0.2.0  # Uncomment if using Mistral  
# groq>=0.4.0  # Uncomment if using Groq

# Optional heavy dependencies
# PyPDF2>=3.0.0  # Only if you need PDF support
# pubchempy>=1.0.4  # Only if you need chemical data
# pix2text>=1.1.0  # Only if you need OCR

# Backend (only if running backend server)
# fastapi>=0.104.0
# uvicorn>=0.24.0
# python-multipart>=0.0.6
